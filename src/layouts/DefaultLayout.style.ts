import { style } from "typestyle";

const DefaultLayoutStyle = {
  common: style({
    height: "100vh",
    width: "100vw",
    overflowX: "hidden",
    overflowY: "hidden",
  }),
};

export default DefaultLayoutStyle;
